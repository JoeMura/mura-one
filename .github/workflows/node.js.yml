# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
const express = require('express');
const multer = require('multer');
const router = express.Router();
const upload = multer({ dest: 'uploads/idcards/' });

// Middleware: ensure user is authenticated (using Passport or JWT)
function ensureAuthenticated(req, res, next) {
  // ...your authentication check logic
  next();
}

router.post('/upload-id', ensureAuthenticated, upload.single('idCard'), async (req, res) => {
  // Update user profile in DB
  const userId = req.user.id;
  const imagePath = req.file.path;
  // Update DB: User.findByIdAndUpdate(userId, { idCardPath: imagePath, verified: false });
  res.json({ success: true, message: 'ID card uploaded. Verification pending.' });
});

module.exports = router;
